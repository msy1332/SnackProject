#include <easyx.h>
#include <iostream>
#include <windows.h>
#include <cmath>

class PutImage
{
    public:
        PutImage();
        ~PutImage();
        void Transparency_Map(int x,int y,IMAGE *img);
    private:
    protected:
};