#include "../Snake/Snake.h"
#include <iostream>
#include <easyx.h>
#pragma once

class Game
{
    public:
        Game();
        ~Game();
        bool Game_End(Snake snake);
    private:
    protected:
};